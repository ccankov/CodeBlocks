import React from 'react';

import NavbarContainer from './navigation/navbar_container';

const App = (props) => (
  <section>
    <NavbarContainer />
    <main>

    </main>
  </section>
);

export default App;
